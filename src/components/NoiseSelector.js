import React from 'react';

const NoiseSelector = () => {
  return (
    <div>
      <h3>Select White Noise & Wake-Up Sounds</h3>
      {/* Add Spotify/Apple Music integration later */}
      <button>Select White Noise</button>
      <button>Select Wake Up Sounds</button>
    </div>
  );
};

export default NoiseSelector;
